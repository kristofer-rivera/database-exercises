SHOW TABLES;
DESCRIBE employees;
-- Different data types available include: numeric, string, date, null, and a primary key. 
-- I think dept_emp, employees, salaries, and titles contain numberic type columns
DESCRIBE departments;
DESCRIBE dept_emp;
-- I think all the tables except salaries will contain string type columns
-- I think all the tables except departments will contain date type columns
-- In the dept_emp table we can see that the employee and department each have a number that correlates to eachother. 
SHOW CREATE TABLE dept_manager;
